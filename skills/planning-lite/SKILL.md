---
name: planning-lite
description: Produce a plan with minimal human interruption — the quick planning lens. Draft the approach, surface only the genuinely consequential decisions in one batched pass, and get a fast sign-off. Use for escalated tasks that need a plan but not a full interrogation; for high-stakes or ambiguous work use planning instead.
---

# Planning (lite)

Draft a plan quickly, pulling the human in only where it matters.

- Draft the approach yourself from the task and the codebase; make the reversible, low-stakes calls without asking.
- Surface only the genuinely consequential or ambiguous decisions — batched into one pass, each with a recommended answer — rather than interrogating every branch.
- Get a single sign-off, then proceed.
- Produce the agreed plan as the output; for an escalated task, save it per the workflow (`docs/plans/<task>.md`).
